# Copy to .env.local for local development. For Netlify, set these in Site settings → Environment variables.

# Supabase (required – used in browser)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# Gemini (for AI word generation / enrichment)
GEMINI_API_KEY=your-gemini-api-key

# Optional: Claude (if using Claude for extraction)
CLAUDE_API_KEY=your-claude-api-key
